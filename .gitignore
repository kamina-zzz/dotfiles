nvim/.config/nvim/lazy-lock.json

# Local/confidential settings directory
# Files in local/ are git-ignored (except .keep)
# Use stow to symlink: stow -t ~/ local
local/*
!local/.keep
!local/.stow-local-ignore
